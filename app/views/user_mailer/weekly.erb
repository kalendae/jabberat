<%=h @user.login.titleize %>, here are the top conversations on Tawk for this week:

  <% @topics.each do |topic| %>
    <%= topic.user.login.titleize %> said: "<%= topic.content%>"
    Join this conversation at: http://www.tawk.com<%= url_for(:controller => :start, :action => :index, :id => topic) %>

  <% end %>

Enjoy!

- Tawk.com

to stop receiving this weekly update, simply uncheck the option at
http://www.tawk.com<%= url_for(:controller => :users, :action => :show, :id => @user) %>

