Hello,

  <%= @user.login %> replied to the conversation topic "<%= @topic.content %>" and said "<%= @comment.content %>"

  To view the whole conversation and to reply goto http://www.tawk.com<%= url_for(:controller => :start, :action => :index, :id => @topic) %>

